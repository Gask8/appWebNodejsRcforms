<%- include('../partials/headless')%>

<body>
  <div class="container" style="margin-top: 75px">
    <% if(wrong && wrong.length) {%>
    <div
      class="alert alert-danger"
      role="alert"
      style="text-align: center; margin-top: 10px"
    >
      <%=wrong%>
    </div>
    <%}%>
    <div class="jumbotron" style="margin-top: 50px">
      <h1 class="text-center" style="font-weight: 700">Error</h1>
      <p class="text-center" style="margin: 25px 0">
        <span
          style="color: #c3412a; font-size: 125px"
          class="glyphicon glyphicon-remove-circle"
        ></span>
      </p>
      <p class="text-center">Perdon por la molestia, Contacte ayuda</p>
      <br />
    </div>
  </div>
</body>
<%- include('../partials/footer')%>
